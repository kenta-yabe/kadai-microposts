<% if current_user.reg_favorites?(reg_post) %>

　<%= link_to "お気に入り解除", favorite_path(id: reg_post.id), method: :delete, class: 'btn btn-danger btn-sm' %>


<% else %>

  <%= link_to "お気に入り登録", favorites_path(id: reg_post.id), method: :post, class: 'btn btn-primary btn-sm' %>


<% end %>

